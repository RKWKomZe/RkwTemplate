plugin.tx_rkwmailer {

    settings {

        // Page for redirect plugin
        redirectPid = 4376

        // Delay for redirect
        redirectDelay = 0

        // Link plugin for pixel counter (HTML-mails only)
        counterPixelPid = 4376

    }
}
