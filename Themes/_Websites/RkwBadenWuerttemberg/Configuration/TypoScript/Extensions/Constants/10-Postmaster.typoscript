plugin.tx_postmaster {

    settings {

        # cat=plugin.tx_postmaster; type=integer; label=Pid of redirect plugin
        redirectPid = 5084

        # cat=plugin.tx_postmaster; type=integer; label=Pid of plugin for pixel counter (HTML-mails only)
        counterPixelPid = 5084
    }
}
