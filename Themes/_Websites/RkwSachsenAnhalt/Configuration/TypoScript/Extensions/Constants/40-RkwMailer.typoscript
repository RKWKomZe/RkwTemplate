plugin.tx_rkwmailer {

    settings {

        // Page for redirect plugin
        redirectPid = 3496

        // Link plugin for pixel counter (HTML-mails only)
        counterPixelPid = 3496
    }
}