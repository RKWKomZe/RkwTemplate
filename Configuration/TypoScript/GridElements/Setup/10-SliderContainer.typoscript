
tt_content.gridelements_pi1.20.10.setup {

    # id of grid element
    sliderContainer < lib.gridelements.defaultGridSetup
    sliderContainer {

        cObject = FLUIDTEMPLATE
        cObject {
            file = {$plugin.tx_rkwtemplate.view.templateRootPath}GridElements/SliderContainer.html
        }
        outerWrap = |
    }
}