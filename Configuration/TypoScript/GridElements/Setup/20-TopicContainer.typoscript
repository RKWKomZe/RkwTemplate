
tt_content.gridelements_pi1.20.10.setup {

    # id of grid element
    topicContainer < lib.gridelements.defaultGridSetup
    topicContainer {

        cObject = FLUIDTEMPLATE
        cObject {
            file = {$plugin.tx_rkwtemplate.view.templateRootPath}GridElements/TopicContainer.html
        }
        outerWrap = |

    }
}