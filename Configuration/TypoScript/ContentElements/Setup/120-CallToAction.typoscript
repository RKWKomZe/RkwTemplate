tt_content {
    rkwtemplate_calltoaction =< lib.contentElement
    rkwtemplate_calltoaction {
        templateName = CallToAction

        // loading images as references
        dataProcessing {
            10 = TYPO3\CMS\Frontend\DataProcessing\FilesProcessor
            10.references.fieldName = image
            10.as = images
        }
    }
}
