tt_content {
    rkwtemplate_textimagebackground =< lib.contentElement
    rkwtemplate_textimagebackground {
        templateName = TextImageBackground

        // loading images as references
        dataProcessing {
            10 = TYPO3\CMS\Frontend\DataProcessing\FilesProcessor
            10.references.fieldName = image
            10.as = images
        }
    }
}