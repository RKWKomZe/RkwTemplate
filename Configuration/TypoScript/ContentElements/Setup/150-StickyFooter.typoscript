tt_content {
    rkwtemplate_stickyfooter =< lib.contentElement
    rkwtemplate_stickyfooter {
        templateName = StickyFooter

        // loading images as references
        dataProcessing {
            10 = TYPO3\CMS\Frontend\DataProcessing\FilesProcessor
            10.references.fieldName = image
            10.as = images
        }
    }
}