tt_content {
    rkwtemplate_logo =< lib.contentElement
    rkwtemplate_logo{
        templateName = Logo

        // loading images as references
        dataProcessing {
            10 = TYPO3\CMS\Frontend\DataProcessing\FilesProcessor
            10.references.fieldName = image
            10.as = images
        }
    }
}