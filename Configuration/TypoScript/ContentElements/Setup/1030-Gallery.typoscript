tt_content {
    rkwtemplate_gallery =< lib.contentElement
    rkwtemplate_gallery {
        templateName = Gallery

        // loading images as references
        dataProcessing {
            10 = TYPO3\CMS\Frontend\DataProcessing\FilesProcessor
            10.references.fieldName = image
            10.as = images
        }
    }
}