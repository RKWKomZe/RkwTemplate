tt_content {
    rkwtemplate_imagelist =< lib.contentElement
    rkwtemplate_imagelist {
        templateName = ImageList

        // loading images as references
        dataProcessing {
            10 = TYPO3\CMS\Frontend\DataProcessing\FilesProcessor
            10.references.fieldName = image
            10.as = images
        }
    }
}