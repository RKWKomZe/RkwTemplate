tt_content {
    rkwtemplate_button =< lib.contentElement
    rkwtemplate_button {
        templateName = Button

        // loading images as references
        dataProcessing {
            10 = TYPO3\CMS\Frontend\DataProcessing\FilesProcessor
            10.references.fieldName = image
            10.as = images
        }
    }
}